import { LeaveHoverClassDirective } from './leave-hover-class.directive';

describe('LeaveHoverClassDirective', () => {
  it('should create an instance', () => {
    const directive = new LeaveHoverClassDirective();
    expect(directive).toBeTruthy();
  });
});
