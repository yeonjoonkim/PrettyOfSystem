import { OnHoverClassDirective } from './on-hover-class.directive';

describe('OnHoverClassDirective', () => {
  it('should create an instance', () => {
    const directive = new OnHoverClassDirective();
    expect(directive).toBeTruthy();
  });
});
