import { GenderIconNamePipe } from './gender-icon-name.pipe';

describe('GenderIconNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GenderIconNamePipe();
    expect(pipe).toBeTruthy();
  });
});
