import { PercentTransformPipe } from './percent-transform.pipe';

describe('PercentTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
